# frozen_string_literal: true

json = {
  uuid: @task.uuid,
  content: @task.content,
  state: @task.state,
  created_at: @task.created_at,
  updated_at: @task.updated_at
}

json[:likes] = @task.user_likes

json
