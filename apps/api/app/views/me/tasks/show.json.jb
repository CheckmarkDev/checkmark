# frozen_string_literal: true

json = {
  uuid: @task.uuid,
  content: @task.content,
  state: @task.state,
  created_at: @task.created_at,
  updated_at: @task.updated_at
}

json[:likes] = @task.task_likes.map do |like|
  User.find_by(id: like.user_id).uuid
end

json
